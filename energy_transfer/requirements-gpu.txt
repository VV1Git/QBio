# GPU acceleration for energy_transfer (CUDA 12.x)
# Install after the base requirements:
#   pip install -r requirements.txt
#   pip install -r requirements-gpu.txt
jax[cuda12]
qutip-jax
diffrax        # JAX ODE integrator for the GPU vibronic Lindblad solver
