# GPU requirements for NVIDIA CUDA 12.x
# Install AFTER requirements.txt:
#   pip install -r requirements.txt
#   pip install -r requirements-gpu.txt

# Level 1 — CuPy (GPU-accelerated spectral density table construction)
# Pick the one matching your CUDA version:
cupy-cuda12x>=13.0      # CUDA 12.x (RTX 30/40 series, most modern GPUs)
# cupy-cuda11x>=12.0    # CUDA 11.x (uncomment if on older CUDA)

# Level 2 — JAX/CUDA (GPU-accelerated QuTiP ODE integration via brmesolve)
# This moves Hilbert-space matrix ops and ODE stepping onto the GPU.
jax[cuda12_pip]>=0.4.20
jaxlib>=0.4.20
qutip-jax>=0.0.4

# To activate JAX backend in your script, add before calling run_fmo():
#   import qutip_jax
#   import qutip as qt
#   qt.settings.core["default_dtype"] = "jaxdia"
